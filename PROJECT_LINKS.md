# Project Links

Linked assets for this EmbeddedX project:
- `model`: `../../modules/embedded-intelligence-layer/models/soot_detector`
- `shared_mrd_library`: `../../mrd_library`

Notes:
- Keep project outputs in this project folder.
- Keep shared model logic in the linked EIL module path.
- Current active firmware bug is tracked in project docs (`README.md`, `docs/PROJECT_STATE.md`).
