# Outputs

Generated artifacts from compare, replay, and diagnostics.

## Structure
- `compare/`: model strategy comparison outputs.
- `replay/`: replay execution results.
- `reports/`: markdown/json summaries for demos and reviews.

## Usage
- Treat outputs as run artifacts, not source-of-truth configuration.
- Keep final demo evidence in `reports/` with timestamps.
